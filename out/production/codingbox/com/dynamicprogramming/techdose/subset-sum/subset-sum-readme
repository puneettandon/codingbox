#Subset Sum Problem


##Given :  
    i) Set of non-negative integers
    ii) Sum Value

##Find:
    If there exists a subset in our given set such that,
    some of elements of subset = sum value

##Example: 
    set=(2,2,3) 
    sum = 5 
    Ans -> true (2,3)

## Approach : 
- ###Backtracking / Recursion:
        isSubSetSum(set,n,sum) = isSubSetSum(set,n-1,sum) || isSubSetSum(set,n-1,sum-set(n-1))
        Example : 
            set = (1,2,3)
            sum = 5
            
        